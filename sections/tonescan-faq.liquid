{%- comment -%}
  Frequently asked questions.

  Block-driven rather than hardcoded. It was nine copies of the same four lines of markup, which
  meant every wording change was a code change, and the answers drifted out of step with the
  pricing and feature sections that are edited in the theme editor. Now it is the same kind of
  section as the rest of the page.

  Page-wide CSS and JS are rendered once by layout/tonescan.liquid, so this file carries markup
  only and can be reordered or removed without taking them with it.
{%- endcomment -%}

<div class="tonescan-landing tw:text-neutral-200">
  <section id="faq" class="tw:scroll-mt-[84px] tw:border-t tw:border-white/10 tw:bg-white/[0.015]">
    <div class="tw:mx-auto tw:w-full tw:max-w-[760px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
      <h2 class="tw:mb-[40px] tw:text-center tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">{{ section.settings.heading }}</h2>
      <div class="tw:flex tw:flex-col tw:gap-[12px]">
        {%- for block in section.blocks -%}
          {%- if block.settings.q != blank -%}
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]" {{ block.shopify_attributes }}>
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">{{ block.settings.q }}<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45" aria-hidden="true">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">{{ block.settings.a }}</p>
        </details>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>
  </section>
</div>

{% schema %}
{
  "name": "toneScan FAQ",
  "tag": "section",
  "class": "section",
  "max_blocks": 14,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Frequently asked questions"
    }
  ],
  "blocks": [
    {
      "type": "qa",
      "name": "Question",
      "settings": [
        {
          "type": "text",
          "id": "q",
          "label": "Question",
          "default": "Question"
        },
        {
          "type": "textarea",
          "id": "a",
          "label": "Answer",
          "default": "Answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "toneScan FAQ"
    }
  ]
}
{% endschema %}
