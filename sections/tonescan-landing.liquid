{%- comment -%}
  tonescan-landing
  ----------------
  Code-first, dark, professional SaaS landing page for the tonescan app.
  Rendered via templates/page.tonescan.liquid using layout/tonescan.liquid
  (real shop logo + theme fonts; no shop header/footer).

  CONVENTIONS:
  - Tailwind v4, prefix-first: tw:flex, tw:md:grid, tw:hover:opacity-90.
  - Dark theme. Accent = BLUE: #3b82f6 (buttons/dots/borders), #60a5fa (eyebrow text).
  - MINIMAL corner radius: tw:rounded-[2px] on cards/frames/buttons/tiles/badges (pro-software vibe).
    rounded-full ONLY on tiny indicator dots.
  - Container matches the main site: max-w-[1400px], px-[15px] md:px-[50px] (logo click doesn't jump).
  - Arbitrary PX for type/spacing (theme root is 62.5%). Layout utils (flex/grid) are safe.
  - COPY RULE: sell tangible benefits, never the tech stack. RGB trichromatic support is the
    key differentiator — it leads.
  - Content is plain inline HTML — edit copy directly.
  - Status: OPEN BETA (free now; perpetual + subscription at launch). "(soon)" = not shipped.
  - Scroll reveal + hero parallax wired in layout/tonescan.liquid (fails safe).
  - Placeholders in [ brackets ] / grey boxes: screenshots, testimonials, prices, real social proof.
{%- endcomment -%}

<div class="tonescan-landing tw:text-neutral-200">

  {%- comment -%} ============================ 1 · HERO ============================ {%- endcomment -%}
  <section class="tw:relative tw:overflow-hidden">
    <div class="tw:pointer-events-none tw:absolute tw:left-1/2 tw:top-[-160px] tw:h-[360px] tw:w-[720px] tw:-translate-x-1/2 tw:rounded-full tw:bg-[#3b82f6]/10 tw:blur-[130px]"></div>

    <div class="ts-hero tw:relative tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:pt-[80px] tw:pb-[64px] tw:md:px-[50px] tw:md:pt-[120px] tw:md:pb-[88px]">
      {%- comment -%}
        Floating sample images — scattered high around the headline, then drift DOWN and inward
        to the screenshot centre (rack into focus) on scroll. --drop tunes how far down they
        travel to reach the hero centre. Swap gradients for real sample scans later.
      {%- endcomment -%}
      <div class="hero-floats tw:hidden tw:md:block" aria-hidden="true">
        <div class="hero-float hero-float--1"></div>
        <div class="hero-float hero-float--2"></div>
        <div class="hero-float hero-float--3"></div>
        <div class="hero-float hero-float--4"></div>
        <div class="hero-float hero-float--5"></div>
        <div class="hero-float hero-float--6"></div>
        <div class="hero-float hero-float--7"></div>
      </div>
      <div class="tw:mx-auto tw:flex tw:max-w-[860px] tw:flex-col tw:items-center tw:gap-[26px] tw:text-center">
        <span class="tw:inline-flex tw:items-center tw:gap-[8px] tw:rounded-[2px] tw:border tw:border-white/15 tw:bg-white/[0.04] tw:px-[12px] tw:py-[6px] tw:text-[13px] tw:font-medium tw:text-neutral-300">
          <span class="tw:h-[6px] tw:w-[6px] tw:rounded-full tw:bg-[#3b82f6]"></span>
          Now in open beta
        </span>

        <h1 class="tw:text-[42px] tw:font-semibold tw:leading-[1.03] tw:tracking-[-0.03em] tw:md:text-[72px]">
          <span class="tw:bg-[linear-gradient(180deg,#ffffff,#a3a3a3)] tw:bg-clip-text tw:text-transparent">
            Film negatives to finished photos, in seconds.
          </span>
        </h1>

        <p class="tw:max-w-[600px] tw:text-[18px] tw:leading-[1.55] tw:text-neutral-400 tw:md:text-[20px]">
          tonescan turns your camera-scanned negatives into gallery-ready images — with
          physically accurate inversions, clean RGB colour, and a workflow built to fly
          through whole rolls.
        </p>

        <div class="tw:mt-[6px] tw:flex tw:flex-wrap tw:items-center tw:justify-center tw:gap-[12px]">
          <a href="#pricing" class="tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6] tw:px-[26px] tw:py-[14px] tw:text-[15px] tw:font-semibold tw:text-white tw:transition-transform tw:hover:-translate-y-[1px]">
            Download the beta
          </a>
          <a href="#how-it-works" class="tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:border tw:border-white/15 tw:bg-white/[0.03] tw:px-[26px] tw:py-[14px] tw:text-[15px] tw:font-medium tw:text-white tw:transition-colors tw:hover:bg-white/[0.08]">
            See how it works
          </a>
        </div>
        <p class="tw:text-[13px] tw:text-neutral-500">Free during the beta · Mac, Windows &amp; Linux (soon)</p>
      </div>

      <div class="ts-hero-frame tw:relative tw:mx-auto tw:mt-[56px] tw:max-w-[1320px] tw:md:mt-[72px]">
        <img
          src="{{ 'tonescan-hero.png' | asset_url }}"
          alt="toneScan Develop view converting an RGB triplet scan"
          width="2000"
          height="1180"
          class="ts-hero-img tw:relative tw:block tw:h-auto tw:w-full"
          loading="eager"
        >
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 2 · SOCIAL PROOF STRIP ==================== {%- endcomment -%}
  <section class="tw:border-y tw:border-white/10">
    <div class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[28px] tw:md:px-[50px]">
      <p class="tw:text-center tw:text-[14px] tw:text-neutral-500">
        From the team behind <span class="tw:text-neutral-300">toneCarrier</span> — trusted by hundreds of film photographers worldwide.
      </p>
    </div>
  </section>

  {%- comment -%} ==================== 3 · PROBLEM / AGITATION ==================== {%- endcomment -%}
  <section class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
    <div class="tw:mx-auto tw:max-w-[720px] tw:text-center">
      <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">The problem</p>
      <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">
        Converting film shouldn't be the hard part.
      </h2>
      <p class="tw:mx-auto tw:mt-[20px] tw:max-w-[560px] tw:text-[17px] tw:leading-[1.6] tw:text-neutral-400">
        You shot the roll. You scanned it. Then the fun stops — wrestling with clunky
        tools, chasing accurate colour, and fighting for consistency frame to frame.
      </p>
    </div>
    <div class="tw:mt-[48px] tw:grid tw:grid-cols-1 tw:gap-[16px] tw:md:grid-cols-3">
      <div class="tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[28px]">
        <div class="tw:mb-[14px] tw:h-[36px] tw:w-[36px] tw:rounded-[2px] tw:bg-white/10"></div>
        <h3 class="tw:mb-[8px] tw:text-[18px] tw:font-semibold tw:text-white">Slow and fiddly</h3>
        <p class="tw:text-[15px] tw:leading-[1.55] tw:text-neutral-400">Endless sliders and plugins just to get one frame looking right.</p>
      </div>
      <div class="tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[28px]">
        <div class="tw:mb-[14px] tw:h-[36px] tw:w-[36px] tw:rounded-[2px] tw:bg-white/10"></div>
        <h3 class="tw:mb-[8px] tw:text-[18px] tw:font-semibold tw:text-white">Inconsistent results</h3>
        <p class="tw:text-[15px] tw:leading-[1.55] tw:text-neutral-400">Every frame comes out looking different from the last.</p>
      </div>
      <div class="tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[28px]">
        <div class="tw:mb-[14px] tw:h-[36px] tw:w-[36px] tw:rounded-[2px] tw:bg-white/10"></div>
        <h3 class="tw:mb-[8px] tw:text-[18px] tw:font-semibold tw:text-white">Hours per roll</h3>
        <p class="tw:text-[15px] tw:leading-[1.55] tw:text-neutral-400">Manual tweaking and exporting turns a hobby into a chore.</p>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 4 · INTERACTIVE DEMO ==================== {%- endcomment -%}
  <section class="tw:relative tw:overflow-hidden tw:border-y tw:border-white/10 tw:bg-white/[0.015]">
    <div class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px] tw:text-center">
      <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">See it work</p>
      <h2 class="tw:mx-auto tw:max-w-[720px] tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">
        Drag to reveal the conversion.
      </h2>
      <div class="tw:mx-auto tw:mt-[40px] tw:max-w-[920px]">
        {%- comment -%}
          Before/after slider. Placeholder gradients now; drop real scans in later by
          replacing the two .ts-ba__img divs with <img> (after = positive, before = negative)
          and removing .ts-ba__img--neg. Driven by a hidden range input (keyboard-accessible).
        {%- endcomment -%}
        <div class="ts-ba" style="--pos:50%">
          <div class="ts-ba__layer ts-ba__after"><div class="ts-ba__img"></div></div>
          <div class="ts-ba__layer ts-ba__before"><div class="ts-ba__img ts-ba__img--neg"></div></div>
          <div class="ts-ba__divider">
            <div class="ts-ba__handle" aria-hidden="true">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 6 3 12l6 6M15 6l6 6-6 6"/></svg>
            </div>
          </div>
          <span class="ts-ba__tag ts-ba__tag--l">Negative</span>
          <span class="ts-ba__tag ts-ba__tag--r">Converted</span>
          <input class="ts-ba__range" type="range" min="0" max="100" value="50" aria-label="Reveal the converted image">
        </div>
        <p class="tw:mt-[16px] tw:text-[13px] tw:text-neutral-500">Drag the handle to compare.</p>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 5 · PLATFORM (bento) ====================
       Framer-style "full platform" bento. RGB is the hero tile.
  {%- endcomment -%}
  <section id="features" class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:scroll-mt-[84px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
    <div class="tw:mx-auto tw:max-w-[760px] tw:text-center">
      <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">The platform</p>
      <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">
        A complete film workflow — not a plugin.
      </h2>
    </div>

    <div class="tw:mt-[48px] tw:grid tw:grid-cols-1 tw:gap-[16px] tw:md:grid-cols-3 tw:md:auto-rows-[210px]">
      {%- comment -%} HERO tile: RGB {%- endcomment -%}
      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[28px] tw:md:col-span-2 tw:md:row-span-2">
        <div class="tw:mb-[22px] tw:flex tw:min-h-[180px] tw:flex-1 tw:items-center tw:justify-center tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-neutral-900 tw:text-[13px] tw:text-neutral-600">[ RGB channel visual ]</div>
        <h3 class="tw:text-[22px] tw:font-semibold tw:text-white">Seamless RGB scanning</h3>
        <p class="tw:mt-[8px] tw:max-w-[460px] tw:text-[15px] tw:leading-[1.55] tw:text-neutral-400">One of the only apps with clean, first-class support for RGB trichromatic capture — separate red, green and blue exposures combined for purer colour and none of the crosstalk of white light.</p>
      </div>

      {%- comment -%} small tiles {%- endcomment -%}
      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[24px]">
        <div class="tw:mb-[16px] tw:flex tw:h-[40px] tw:w-[40px] tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6]/15"><div class="tw:h-[16px] tw:w-[16px] tw:rounded-[1px] tw:bg-[#3b82f6]"></div></div>
        <div class="tw:mt-auto">
          <h3 class="tw:text-[17px] tw:font-semibold tw:text-white">Instant previews</h3>
          <p class="tw:mt-[6px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Real-time GPU viewport — what you see is what you export.</p>
        </div>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[24px]">
        <div class="tw:mb-[16px] tw:flex tw:h-[40px] tw:w-[40px] tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6]/15"><div class="tw:h-[16px] tw:w-[16px] tw:rounded-[1px] tw:bg-[#3b82f6]"></div></div>
        <div class="tw:mt-auto">
          <h3 class="tw:text-[17px] tw:font-semibold tw:text-white">Automatic batch</h3>
          <p class="tw:mt-[6px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Drag in a whole roll and convert every frame in one pass.</p>
        </div>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[24px]">
        <div class="tw:mb-[16px] tw:flex tw:h-[40px] tw:w-[40px] tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6]/15"><div class="tw:h-[16px] tw:w-[16px] tw:rounded-[1px] tw:bg-[#3b82f6]"></div></div>
        <div class="tw:mt-auto">
          <h3 class="tw:text-[17px] tw:font-semibold tw:text-white">Physically accurate inversion</h3>
          <p class="tw:mt-[6px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Natural, true colour — consistent across the whole roll.</p>
        </div>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[24px]">
        <div class="tw:mb-[16px] tw:flex tw:h-[40px] tw:w-[40px] tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6]/15"><div class="tw:h-[16px] tw:w-[16px] tw:rounded-[1px] tw:bg-[#3b82f6]"></div></div>
        <div class="tw:mt-auto">
          <h3 class="tw:text-[17px] tw:font-semibold tw:text-white">True exposure from raw</h3>
          <p class="tw:mt-[6px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Calibrated from real raw signal, not the histogram.</p>
        </div>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[24px]">
        <div class="tw:mb-[16px] tw:flex tw:h-[40px] tw:w-[40px] tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6]/15"><div class="tw:h-[16px] tw:w-[16px] tw:rounded-[1px] tw:bg-[#3b82f6]"></div></div>
        <div class="tw:mt-auto">
          <h3 class="tw:text-[17px] tw:font-semibold tw:text-white">Rich metadata</h3>
          <p class="tw:mt-[6px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Film stock, camera and dev info tagged into every file.</p>
        </div>
      </div>
    </div>

    {%- comment -%} secondary "and more" grid {%- endcomment -%}
    <div class="tw:mt-[16px] tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.015] tw:p-[32px] tw:md:p-[40px]">
      <h3 class="tw:mb-[28px] tw:text-center tw:text-[20px] tw:font-semibold tw:text-white">Plus everything a serious workflow needs</h3>
      <div class="tw:grid tw:grid-cols-1 tw:gap-x-[40px] tw:gap-y-[20px] tw:sm:grid-cols-2 tw:md:grid-cols-3">
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Hardware control</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Drives your light source and advances the film carrier for you.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">White-light scan support</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Prefer a single-shot light source? Fully supported too.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Smart auto white balance</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Neutral, accurate colour without manual picking.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Flat-field correction</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Even illumination edge to edge across every frame.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Keyboard-driven</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Shortcuts for everything, so you can fly through a roll.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Fully standalone</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">No Lightroom, no plugins, no host app required.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span>
          <div><p class="tw:text-[15px] tw:font-semibold tw:text-white">Flexible licensing</p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Own it outright or subscribe — your choice.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-white/25"></span>
          <div><p class="tw:flex tw:items-center tw:gap-[8px] tw:text-[15px] tw:font-semibold tw:text-white">Linux support <span class="tw:rounded-[2px] tw:border tw:border-white/15 tw:px-[8px] tw:py-[1px] tw:text-[11px] tw:font-medium tw:uppercase tw:tracking-[0.08em] tw:text-neutral-500">Soon</span></p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Native on Mac and Windows today; Linux on the way.</p></div>
        </div>
        <div class="tw:flex tw:gap-[12px]">
          <span class="tw:mt-[7px] tw:h-[7px] tw:w-[7px] tw:shrink-0 tw:rounded-full tw:bg-white/25"></span>
          <div><p class="tw:flex tw:items-center tw:gap-[8px] tw:text-[15px] tw:font-semibold tw:text-white">AI auto-crop <span class="tw:rounded-[2px] tw:border tw:border-white/15 tw:px-[8px] tw:py-[1px] tw:text-[11px] tw:font-medium tw:uppercase tw:tracking-[0.08em] tw:text-neutral-500">Soon</span></p><p class="tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Automatic frame detection and cropping, powered by machine learning.</p></div>
        </div>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 6 · WORKFLOW (sticky text, scrolling media) ====================
       Desktop: text pinned + vertically centred on the left, crossfading as the media panels
       scroll on the right. Mobile: stacked (media + inline text per step).
       Swap .ts-story__v0 / .ts-story__v3 gradients for real capture / export screenshots.
  {%- endcomment -%}
  <section id="how-it-works" class="tw:scroll-mt-[84px] tw:border-y tw:border-white/10 tw:bg-white/[0.015]">
    <div class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
      <div class="tw:mx-auto tw:max-w-[720px] tw:text-center">
        <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">The workflow</p>
        <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">One workflow, from capture to export.</h2>
      </div>

      <div class="ts-story tw:mt-[40px] tw:md:mt-[56px]">
        {%- comment -%} LEFT: pinned, vertically-centred text that crossfades (desktop) {%- endcomment -%}
        <div class="ts-story__textcol tw:hidden tw:md:block">
          <div class="ts-story__sticky">
            <div class="ts-story__textwrap">
              <div class="ts-story__text is-active" data-i="0">
                <span class="ts-story__num">01 — Capture</span>
                <h3 class="ts-story__title">Capture your roll, hands-free</h3>
                <p class="ts-story__body">Capture in seamless RGB or single-shot white light. tonescan can drive your light source and advance the film carrier, so an entire roll flows through without touching a thing.</p>
              </div>
              <div class="ts-story__text" data-i="1">
                <span class="ts-story__num">02 — Convert</span>
                <h3 class="ts-story__title">Physically accurate conversion</h3>
                <p class="ts-story__body">Physically-based inversion reconstructs natural, faithful colour from your channels — the same look from the first frame to the last.</p>
              </div>
              <div class="ts-story__text" data-i="2">
                <span class="ts-story__num">03 — Refine</span>
                <h3 class="ts-story__title">Refine in real time</h3>
                <p class="ts-story__body">A GPU-accelerated viewport shows pixel-perfect previews the instant you adjust exposure, white balance and flat-field correction.</p>
              </div>
              <div class="ts-story__text" data-i="3">
                <span class="ts-story__num">04 — Export</span>
                <h3 class="ts-story__title">Export, catalogue-ready</h3>
                <p class="ts-story__body">Export gallery-ready images, automatically tagged with film stock, camera and development details — organised and ready for your catalogue.</p>
              </div>
            </div>
          </div>
        </div>

        {%- comment -%} RIGHT: scrolling media panels (each drives the active text) {%- endcomment -%}
        <div class="ts-story__mediacol">
          <div class="ts-story__panel" data-i="0">
            <div class="ts-story__frame ts-story__v0"><span class="ts-story__tag">Capture</span></div>
            <div class="ts-story__text ts-story__text--inline tw:md:hidden">
              <span class="ts-story__num">01 — Capture</span>
              <h3 class="ts-story__title">Capture your roll, hands-free</h3>
              <p class="ts-story__body">Capture in seamless RGB or single-shot white light. tonescan can drive your light source and advance the film carrier, so an entire roll flows through without touching a thing.</p>
            </div>
          </div>
          <div class="ts-story__panel" data-i="1">
            <div class="ts-story__frame ts-story__v1"><span class="ts-story__tag">Convert</span></div>
            <div class="ts-story__text ts-story__text--inline tw:md:hidden">
              <span class="ts-story__num">02 — Convert</span>
              <h3 class="ts-story__title">Physically accurate conversion</h3>
              <p class="ts-story__body">Physically-based inversion reconstructs natural, faithful colour from your channels — the same look from the first frame to the last.</p>
            </div>
          </div>
          <div class="ts-story__panel" data-i="2">
            <div class="ts-story__frame ts-story__v2"><span class="ts-story__tag">Refine</span></div>
            <div class="ts-story__text ts-story__text--inline tw:md:hidden">
              <span class="ts-story__num">03 — Refine</span>
              <h3 class="ts-story__title">Refine in real time</h3>
              <p class="ts-story__body">A GPU-accelerated viewport shows pixel-perfect previews the instant you adjust exposure, white balance and flat-field correction.</p>
            </div>
          </div>
          <div class="ts-story__panel" data-i="3">
            <div class="ts-story__frame ts-story__v3"><span class="ts-story__tag">Export</span></div>
            <div class="ts-story__text ts-story__text--inline tw:md:hidden">
              <span class="ts-story__num">04 — Export</span>
              <h3 class="ts-story__title">Export, catalogue-ready</h3>
              <p class="ts-story__body">Export gallery-ready images, automatically tagged with film stock, camera and development details — organised and ready for your catalogue.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 7 · FEATURE DEEP-DIVES (RGB leads) ==================== {%- endcomment -%}
  <section class="ts-clip-x tw:mx-auto tw:flex tw:w-full tw:max-w-[1400px] tw:flex-col tw:gap-[80px] tw:px-[15px] tw:py-[80px] tw:md:gap-[120px] tw:md:px-[50px] tw:md:py-[120px]">
    <div class="tw:grid tw:grid-cols-1 tw:items-center tw:gap-[40px] tw:md:grid-cols-2">
      <div>
        <p class="tw:mb-[14px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">RGB workflow</p>
        <h2 class="tw:text-[28px] tw:font-semibold tw:leading-[1.15] tw:tracking-[-0.02em] tw:text-white tw:md:text-[38px]">The cleanest RGB workflow around</h2>
        <p class="tw:mt-[16px] tw:text-[17px] tw:leading-[1.6] tw:text-neutral-400">RGB trichromatic scanning captures red, green and blue as separate exposures, then combines them for colour separation that white-light scanning simply can't match. Very few tools support it properly — tonescan makes it a first-class, seamless part of the workflow.</p>
      </div>
      <div>
        {%- comment -%} R/G/B channels merge then invert on click. Swap the 3 .ts-rgb__ch backgrounds for real channel images later. {%- endcomment -%}
        <div class="ts-rgb">
          <div class="ts-rgb__stage">
            <div class="ts-rgb__ch ts-rgb__ch--r"></div>
            <div class="ts-rgb__ch ts-rgb__ch--g"></div>
            <div class="ts-rgb__ch ts-rgb__ch--b"></div>
            {%- comment -%}
              The REAL converted output crossfades in at the end (screen-blend is only a
              transition — the engine is the point). Placeholder gradient for now; swap to the
              real image: replace this div with <img class="ts-rgb__result" src="..."> OR set
              its background-image to the converted DSCF0032_RGB result.
            {%- endcomment -%}
            <div class="ts-rgb__result"></div>
          </div>
          <span class="ts-rgb__cap">Three RGB channel captures</span>
        </div>
      </div>
    </div>
    <div class="tw:grid tw:grid-cols-1 tw:items-center tw:gap-[40px] tw:md:grid-cols-2">
      <div class="tw:md:order-2">
        <p class="tw:mb-[14px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">Real-time editing</p>
        <h2 class="tw:text-[28px] tw:font-semibold tw:leading-[1.15] tw:tracking-[-0.02em] tw:text-white tw:md:text-[38px]">Edit at the speed of thought</h2>
        <p class="tw:mt-[16px] tw:text-[17px] tw:leading-[1.6] tw:text-neutral-400">Every slider, every tweak — reflected the instant you make it. A GPU-accelerated viewport renders pixel-perfect previews in real time, so the result on screen is exactly what you export.</p>
      </div>
      <div class="tw:md:order-1">
        <div class="tw:flex tw:aspect-[4/3] tw:w-full tw:items-center tw:justify-center tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-neutral-900 tw:text-[13px] tw:text-neutral-600">[ viewport screenshot ]</div>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 8 · SAMPLE SCANS (gallery + lightbox) ====================
       Placeholder gradients + example metadata. To use real scans: set data-img="<url>" on
       each .ts-scan button (the lightbox will render an <img>), and update the data-* fields.
  {%- endcomment -%}
  <section id="gallery" class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:scroll-mt-[84px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
    <div class="tw:mx-auto tw:max-w-[720px] tw:text-center">
      <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">Sample scans</p>
      <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">Real scans, real detail.</h2>
      <p class="tw:mx-auto tw:mt-[16px] tw:max-w-[560px] tw:text-[16px] tw:text-neutral-400">Every image below was converted with tonescan. Click any scan to view it full-screen with the capture details.</p>
    </div>

    <div class="ts-gallery tw:mt-[48px] tw:grid tw:grid-cols-2 tw:gap-[16px] tw:md:grid-cols-3">
      <button type="button" class="ts-scan" style="--h:0deg" data-camera="Fujifilm X-T5" data-lens="Helios 44-2" data-light="Cinestill CS-Lite" data-film="Kodak Gold 200" data-shot="Mamiya 645" data-format="120" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>Kodak Gold 200</span><span class="ts-scan__fmt">120</span></span>
      </button>
      <button type="button" class="ts-scan" style="--h:40deg" data-camera="Fujifilm X-T5" data-lens="Helios 44-2" data-light="Cinestill CS-Lite" data-film="Kodak ColorPlus 200" data-shot="Canon AE-1P" data-format="135" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>Kodak ColorPlus 200</span><span class="ts-scan__fmt">135</span></span>
      </button>
      <button type="button" class="ts-scan" style="--h:320deg" data-camera="Fujifilm X-T5" data-lens="Helios 44-2" data-light="Cinestill CS-Lite" data-film="Lomography Tiger 200" data-shot="Lomomatic 110" data-format="110" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>Lomography Tiger 200</span><span class="ts-scan__fmt">110</span></span>
      </button>
      <button type="button" class="ts-scan" style="--h:85deg" data-camera="Sony A7 IV" data-lens="Nikon Micro 60mm" data-light="Kaiser Slimlite" data-film="Kodak Portra 400" data-shot="Nikon FM2" data-format="135" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>Kodak Portra 400</span><span class="ts-scan__fmt">135</span></span>
      </button>
      <button type="button" class="ts-scan" style="--h:210deg" data-camera="Fujifilm X-T5" data-lens="Sigma 70mm Macro" data-light="Cinestill CS-Lite" data-film="CineStill 800T" data-shot="Leica M6" data-format="135" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>CineStill 800T</span><span class="ts-scan__fmt">135</span></span>
      </button>
      <button type="button" class="ts-scan" style="--h:160deg" data-camera="Sony A7 IV" data-lens="Nikon Micro 60mm" data-light="Kaiser Slimlite" data-film="Ilford HP5 Plus" data-shot="Hasselblad 500C/M" data-format="120" data-img="">
        <span class="ts-scan__img"></span>
        <span class="ts-scan__cap"><span>Ilford HP5 Plus</span><span class="ts-scan__fmt">120</span></span>
      </button>
    </div>

    {%- comment -%} lightbox {%- endcomment -%}
    <div class="ts-lb" hidden>
      <div class="ts-lb__backdrop" data-close></div>
      <div class="ts-lb__panel" role="dialog" aria-modal="true" aria-label="Scan details">
        <button class="ts-lb__close" type="button" aria-label="Close" data-close>&times;</button>
        <div class="ts-lb__stage">
          <button class="ts-lb__nav ts-lb__prev" type="button" aria-label="Previous scan">&lsaquo;</button>
          <div class="ts-lb__imgwrap"></div>
          <button class="ts-lb__nav ts-lb__next" type="button" aria-label="Next scan">&rsaquo;</button>
        </div>
        <dl class="ts-lb__meta">
          <div class="ts-lb__row"><dt>Scanning camera</dt><dd data-k="camera"></dd></div>
          <div class="ts-lb__row"><dt>Lens</dt><dd data-k="lens"></dd></div>
          <div class="ts-lb__row"><dt>Light source</dt><dd data-k="light"></dd></div>
          <div class="ts-lb__row"><dt>Film stock</dt><dd data-k="film"></dd></div>
          <div class="ts-lb__row"><dt>Shot on</dt><dd data-k="shot"></dd></div>
          <div class="ts-lb__row"><dt>Format</dt><dd data-k="format"></dd></div>
        </dl>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 9 · TESTIMONIALS (placeholder) ==================== {%- endcomment -%}
  <section class="tw:border-y tw:border-white/10 tw:bg-white/[0.015]">
    <div class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
      <div class="tw:mx-auto tw:max-w-[720px] tw:text-center">
        <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">Loved by film shooters.</h2>
      </div>
      <div class="tw:mt-[48px] tw:grid tw:grid-cols-1 tw:gap-[16px] tw:md:grid-cols-3">
        {%- for i in (1..3) -%}
          <figure class="tw:flex tw:flex-col tw:gap-[16px] tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[28px]">
            <blockquote class="tw:text-[16px] tw:leading-[1.6] tw:text-neutral-200">"[ Real testimonial {{ i }} goes here. ]"</blockquote>
            <figcaption class="tw:flex tw:items-center tw:gap-[12px]">
              <div class="tw:h-[40px] tw:w-[40px] tw:rounded-[2px] tw:bg-white/10"></div>
              <div>
                <p class="tw:text-[14px] tw:font-semibold tw:text-white">[ Name ]</p>
                <p class="tw:text-[13px] tw:text-neutral-500">[ Detail ]</p>
              </div>
            </figcaption>
          </figure>
        {%- endfor -%}
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 9 · PRICING ==================== {%- endcomment -%}
  <section id="pricing" class="tw:mx-auto tw:w-full tw:max-w-[1400px] tw:scroll-mt-[84px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
    <div class="tw:mx-auto tw:max-w-[720px] tw:text-center">
      <p class="tw:mb-[16px] tw:text-[13px] tw:font-semibold tw:uppercase tw:tracking-[0.16em] tw:text-[#60a5fa]">Pricing</p>
      <h2 class="tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">Free while in beta. Flexible licensing at launch.</h2>
      <p class="tw:mx-auto tw:mt-[16px] tw:max-w-[520px] tw:text-[16px] tw:text-neutral-400">tonescan is free to use during the open beta. When we launch, pick the licence that fits — own it forever, or subscribe.</p>
    </div>
    <div class="tw:mt-[48px] tw:grid tw:grid-cols-1 tw:gap-[16px] tw:md:grid-cols-3">

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[32px]">
        <h3 class="tw:text-[20px] tw:font-semibold tw:text-white">Open beta</h3>
        <div class="tw:mt-[12px] tw:flex tw:items-baseline tw:gap-[8px]">
          <span class="tw:text-[40px] tw:font-semibold tw:tracking-[-0.02em] tw:text-white">Free</span>
          <span class="tw:text-[14px] tw:text-neutral-500">right now</span>
        </div>
        <p class="tw:mt-[10px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Every feature unlocked while we're in beta.</p>
        <ul class="tw:mt-[24px] tw:flex tw:flex-1 tw:flex-col tw:gap-[12px] tw:text-[14px] tw:text-neutral-300">
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>Full conversion &amp; editing</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>Mac &amp; Windows</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>Help shape what ships</span></li>
        </ul>
        <a href="#" class="tw:mt-[28px] tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6] tw:px-[24px] tw:py-[13px] tw:text-[15px] tw:font-semibold tw:text-white tw:transition-transform tw:hover:-translate-y-[1px]">Download the beta</a>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-[#3b82f6]/60 tw:bg-[#3b82f6]/[0.06] tw:p-[32px]">
        <span class="tw:mb-[16px] tw:inline-flex tw:w-fit tw:items-center tw:rounded-[2px] tw:bg-[#3b82f6] tw:px-[12px] tw:py-[4px] tw:text-[12px] tw:font-semibold tw:uppercase tw:tracking-[0.08em] tw:text-white">Best value</span>
        <h3 class="tw:text-[20px] tw:font-semibold tw:text-white">Perpetual</h3>
        <div class="tw:mt-[12px] tw:flex tw:items-baseline tw:gap-[8px]">
          <span class="tw:text-[40px] tw:font-semibold tw:tracking-[-0.02em] tw:text-white">[ $ ]</span>
          <span class="tw:text-[14px] tw:text-neutral-500">one-time · at launch</span>
        </div>
        <p class="tw:mt-[10px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Own it forever. Yours to keep.</p>
        <ul class="tw:mt-[24px] tw:flex tw:flex-1 tw:flex-col tw:gap-[12px] tw:text-[14px] tw:text-neutral-300">
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 1 ]</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 2 ]</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 3 ]</span></li>
        </ul>
        <a href="#" class="tw:mt-[28px] tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6] tw:px-[24px] tw:py-[13px] tw:text-[15px] tw:font-semibold tw:text-white tw:transition-transform tw:hover:-translate-y-[1px]">Get perpetual</a>
      </div>

      <div class="tw:flex tw:flex-col tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[32px]">
        <h3 class="tw:text-[20px] tw:font-semibold tw:text-white">Subscription</h3>
        <div class="tw:mt-[12px] tw:flex tw:items-baseline tw:gap-[8px]">
          <span class="tw:text-[40px] tw:font-semibold tw:tracking-[-0.02em] tw:text-white">[ $ ]</span>
          <span class="tw:text-[14px] tw:text-neutral-500">per month · at launch</span>
        </div>
        <p class="tw:mt-[10px] tw:text-[14px] tw:leading-[1.5] tw:text-neutral-400">Always up to date, cancel anytime.</p>
        <ul class="tw:mt-[24px] tw:flex tw:flex-1 tw:flex-col tw:gap-[12px] tw:text-[14px] tw:text-neutral-300">
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 1 ]</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 2 ]</span></li>
          <li class="tw:flex tw:items-start tw:gap-[10px]"><span class="tw:mt-[6px] tw:h-[6px] tw:w-[6px] tw:shrink-0 tw:rounded-full tw:bg-[#3b82f6]"></span><span>[ Feature line 3 ]</span></li>
        </ul>
        <a href="#" class="tw:mt-[28px] tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:border tw:border-white/15 tw:bg-white/[0.03] tw:px-[24px] tw:py-[13px] tw:text-[15px] tw:font-semibold tw:text-white tw:transition-transform tw:hover:-translate-y-[1px]">Get subscription</a>
      </div>

    </div>
  </section>

  {%- comment -%} ==================== 10 · FAQ ==================== {%- endcomment -%}
  <section id="faq" class="tw:scroll-mt-[84px] tw:border-t tw:border-white/10 tw:bg-white/[0.015]">
    <div class="tw:mx-auto tw:w-full tw:max-w-[760px] tw:px-[15px] tw:py-[80px] tw:md:px-[50px] tw:md:py-[120px]">
      <h2 class="tw:mb-[40px] tw:text-center tw:text-[32px] tw:font-semibold tw:leading-[1.1] tw:tracking-[-0.02em] tw:text-white tw:md:text-[46px]">Frequently asked questions</h2>
      <div class="tw:flex tw:flex-col tw:gap-[12px]">
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]">
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">Is it really free right now?<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">Yes — tonescan is completely free to use during the open beta, with every feature unlocked. Paid licences (perpetual or subscription) arrive at launch.</p>
        </details>
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]">
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">Do I need special hardware?<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">No — tonescan works with raw scans from any digital camera. It pairs perfectly with a toneCarrier setup and can even drive its light and carrier, but that isn't required.</p>
        </details>
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]">
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">What film can I convert?<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">Colour negatives, black &amp; white, and slides — across common formats.</p>
        </details>
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]">
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">Does it run on my computer?<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">Yes — tonescan runs natively on Mac and Windows today, with Linux support coming soon.</p>
        </details>
        <details class="tw:group tw:rounded-[2px] tw:border tw:border-white/10 tw:bg-white/[0.02] tw:p-[20px]">
          <summary class="tw:flex tw:cursor-pointer tw:list-none tw:items-center tw:justify-between tw:text-[16px] tw:font-medium tw:text-white">What's the difference between perpetual and subscription?<span class="tw:ml-[16px] tw:text-neutral-500 tw:transition-transform tw:group-open:rotate-45">+</span></summary>
          <p class="tw:mt-[12px] tw:text-[15px] tw:leading-[1.6] tw:text-neutral-400">Perpetual is a one-time purchase you own forever. Subscription keeps you on the latest version and can be cancelled anytime.</p>
        </details>
      </div>
    </div>
  </section>

  {%- comment -%} ==================== 11 · FINAL CTA ==================== {%- endcomment -%}
  <section class="tw:relative tw:overflow-hidden">
    <div class="tw:pointer-events-none tw:absolute tw:left-1/2 tw:bottom-[-180px] tw:h-[360px] tw:w-[720px] tw:-translate-x-1/2 tw:rounded-full tw:bg-[#3b82f6]/10 tw:blur-[130px]"></div>
    <div class="tw:relative tw:mx-auto tw:w-full tw:max-w-[1400px] tw:px-[15px] tw:py-[96px] tw:md:px-[50px] tw:md:py-[140px] tw:text-center">
      <h2 class="tw:mx-auto tw:max-w-[720px] tw:text-[34px] tw:font-semibold tw:leading-[1.08] tw:tracking-[-0.02em] tw:text-white tw:md:text-[56px]">
        Join the tonescan open beta.
      </h2>
      <p class="tw:mx-auto tw:mt-[16px] tw:max-w-[520px] tw:text-[17px] tw:text-neutral-400">Free while in beta. Mac, Windows &amp; Linux (soon).</p>
      <div class="tw:mt-[32px] tw:flex tw:flex-wrap tw:items-center tw:justify-center tw:gap-[12px]">
        <a href="#" class="tw:inline-flex tw:items-center tw:justify-center tw:rounded-[2px] tw:bg-[#3b82f6] tw:px-[28px] tw:py-[15px] tw:text-[15px] tw:font-semibold tw:text-white tw:transition-transform tw:hover:-translate-y-[1px]">
          Download the beta
        </a>
      </div>
    </div>
  </section>

</div>

{%- comment -%} ===== before/after slider: styles + init ===== {%- endcomment -%}
{%- style -%}
  .ts-ba { position: relative; width: 100%; aspect-ratio: 16 / 9; overflow: hidden; border: 1px solid rgba(255,255,255,.10); border-radius: 2px; background: #0f0f10; touch-action: none; user-select: none; }
  .ts-ba__layer { position: absolute; inset: 0; }
  .ts-ba__before { clip-path: inset(0 calc(100% - var(--pos, 50%)) 0 0); }
  .ts-ba__img { width: 100%; height: 100%;
    background:
      radial-gradient(120% 80% at 18% 12%, #7fb1dc 0%, transparent 55%),
      radial-gradient(130% 90% at 88% 100%, #223a52 0%, transparent 60%),
      linear-gradient(135deg, #13324f 0%, #27512f 55%, #3f5a26 100%); }
  .ts-ba__img--neg { filter: invert(1) hue-rotate(180deg) saturate(1.25) contrast(.95); }
  .ts-ba img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; display: block; }
  .ts-ba__divider { position: absolute; top: 0; bottom: 0; left: var(--pos, 50%); width: 2px; margin-left: -1px; background: rgba(255,255,255,.92); pointer-events: none; }
  .ts-ba__handle { position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%); width: 40px; height: 40px; border-radius: 9999px; background: #3b82f6; color: #fff; display: flex; align-items: center; justify-content: center; box-shadow: 0 2px 12px rgba(0,0,0,.45); }
  .ts-ba__tag { position: absolute; top: 12px; padding: 4px 8px; font-size: 11px; letter-spacing: .08em; text-transform: uppercase; color: #fff; background: rgba(0,0,0,.55); border: 1px solid rgba(255,255,255,.15); border-radius: 2px; pointer-events: none; }
  .ts-ba__tag--l { left: 12px; }
  .ts-ba__tag--r { right: 12px; }
  .ts-ba__range { position: absolute; inset: 0; width: 100%; height: 100%; margin: 0; opacity: 0; cursor: ew-resize; }
  .ts-ba__range:focus-visible { outline: 2px solid #3b82f6; outline-offset: -2px; }

  /* ---- scan gallery ---- */
  .ts-scan { position: relative; display: block; width: 100%; aspect-ratio: 3 / 2; overflow: hidden; padding: 0; border: 1px solid rgba(255,255,255,.10); border-radius: 2px; background: #0f0f10; cursor: zoom-in; }
  .ts-scan__img { position: absolute; inset: 0; display: block;
    background:
      radial-gradient(120% 80% at 18% 12%, #7fb1dc 0%, transparent 55%),
      radial-gradient(130% 90% at 88% 100%, #223a52 0%, transparent 60%),
      linear-gradient(135deg, #13324f 0%, #27512f 55%, #3f5a26 100%);
    filter: hue-rotate(var(--h, 0deg)); transition: transform .45s ease; }
  .ts-scan:hover .ts-scan__img { transform: scale(1.04); }
  .ts-scan__cap { position: absolute; inset: auto 0 0 0; display: flex; justify-content: space-between; gap: 8px; padding: 10px 12px; font-size: 12px; color: #fff; background: linear-gradient(to top, rgba(0,0,0,.72), transparent); opacity: 0; transition: opacity .25s ease; }
  .ts-scan:hover .ts-scan__cap, .ts-scan:focus-visible .ts-scan__cap { opacity: 1; }
  .ts-scan__fmt { color: #9db7d6; }
  .ts-scan:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }

  /* ---- lightbox ---- */
  .ts-lb { position: fixed; inset: 0; z-index: 100; display: flex; align-items: center; justify-content: center; padding: 24px; }
  .ts-lb[hidden] { display: none; }
  .ts-lb__backdrop { position: absolute; inset: 0; background: rgba(0,0,0,.85); backdrop-filter: blur(4px); }
  .ts-lb__panel { position: relative; z-index: 1; display: flex; flex-direction: column; width: min(1100px, 100%); max-height: 90vh; overflow: hidden; background: #0f0f10; border: 1px solid rgba(255,255,255,.12); border-radius: 2px; }
  .ts-lb__stage { position: relative; flex: 1; min-height: 300px; background: #0f0f10; }
  .ts-lb__imgwrap { position: absolute; inset: 0; }
  .ts-lb__imgwrap img { width: 100%; height: 100%; object-fit: contain; display: block; }
  .ts-lb__meta { width: 100%; margin: 0; padding: 16px 24px; border-top: 1px solid rgba(255,255,255,.10); }
  .ts-lb__row { display: flex; flex-direction: column; gap: 2px; padding: 9px 0; border-bottom: 1px solid rgba(255,255,255,.06); }
  .ts-lb__row:last-child { border-bottom: 0; }
  .ts-lb__row dt { font-size: 11px; letter-spacing: .08em; text-transform: uppercase; color: #8a8a8a; }
  .ts-lb__row dd { margin: 0; font-size: 15px; color: #fff; }
  .ts-lb__close { position: absolute; top: 10px; right: 10px; z-index: 3; width: 34px; height: 34px; border-radius: 2px; background: rgba(0,0,0,.5); border: 1px solid rgba(255,255,255,.15); color: #fff; font-size: 20px; line-height: 1; cursor: pointer; }
  .ts-lb__nav { position: absolute; top: 50%; transform: translateY(-50%); z-index: 3; width: 40px; height: 40px; border-radius: 9999px; background: rgba(0,0,0,.55); border: 1px solid rgba(255,255,255,.15); color: #fff; font-size: 22px; line-height: 1; cursor: pointer; }
  .ts-lb__prev { left: 12px; }
  .ts-lb__next { right: 12px; }
  .ts-lb__close:focus-visible, .ts-lb__nav:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }
  @media (min-width: 900px) {
    .ts-lb__panel { flex-direction: row; }
    .ts-lb__meta { width: 300px; flex-shrink: 0; border-top: 0; border-left: 1px solid rgba(255,255,255,.10); padding: 24px; }
  }

  /* let the splayed channels overflow their frame without causing horizontal page scroll */
  .ts-clip-x { overflow-x: clip; }

  /* ---- rgb merge (scroll-linked --s spread; .is-converted crossfades the result) ---- */
  .ts-rgb { position: relative; width: 100%; aspect-ratio: 3 / 2; overflow: visible; }
  .ts-rgb__stage { position: absolute; inset: 0; isolation: isolate; }
  .ts-rgb__ch { position: absolute; inset: 8%; mix-blend-mode: screen; background-size: cover; background-position: center; transform-origin: center; will-change: transform; }
  .ts-rgb__ch--r { background-image: url('{{ 'raw-red.jpg' | asset_url }}'); transform: translate(calc(-15% * var(--s, 1)), calc(-9% * var(--s, 1))) rotate(calc(-2deg * var(--s, 1))); }
  .ts-rgb__ch--g { background-image: url('{{ 'raw-green.jpg' | asset_url }}'); transform: translateY(calc(10% * var(--s, 1))); }
  .ts-rgb__ch--b { background-image: url('{{ 'raw-blue.jpg' | asset_url }}'); transform: translate(calc(15% * var(--s, 1)), calc(-9% * var(--s, 1))) rotate(calc(2deg * var(--s, 1))); }
  /* real converted output — crossfades in all at once once the merge completes */
  .ts-rgb__result { position: absolute; inset: 8%; z-index: 1; opacity: 0; transition: opacity .55s ease; background-size: cover; background-position: center; will-change: opacity;
    background-image: url('{{ 'converted.jpg' | asset_url }}'); }
  .ts-rgb.is-converted .ts-rgb__result { opacity: 1; }
  .ts-rgb__cap { position: absolute; left: 12px; bottom: 12px; z-index: 2; padding: 5px 10px; font-size: 12px; color: #fff; background: rgba(0,0,0,.55); border: 1px solid rgba(255,255,255,.15); border-radius: 2px; }

  /* ---- workflow story: sticky centred text (left) + scrolling media (right) ---- */
  .ts-story__frame { position: relative; aspect-ratio: 4 / 3; overflow: hidden; border: 1px solid rgba(255,255,255,.10); border-radius: 2px; background: #0f0f10; background-size: cover; background-position: center; }
  .ts-story__v0 { background-image: linear-gradient(140deg, #16263a 0%, #3a2f1c 100%); }
  .ts-story__v1 { background-image: url('{{ 'converted.jpg' | asset_url }}'); }
  .ts-story__v2 { background-image: url('{{ 'tonescan-hero.png' | asset_url }}'); }
  .ts-story__v3 { background-image: linear-gradient(140deg, #241a3a 0%, #17302b 100%); }
  .ts-story__tag { position: absolute; left: 12px; bottom: 12px; padding: 5px 10px; font-size: 12px; color: #fff; background: rgba(0,0,0,.55); border: 1px solid rgba(255,255,255,.15); border-radius: 2px; }
  .ts-story__num { display: block; margin-bottom: 12px; font-size: 12px; font-weight: 600; letter-spacing: .08em; text-transform: uppercase; color: #60a5fa; }
  .ts-story__title { font-size: 24px; line-height: 1.15; font-weight: 600; letter-spacing: -.01em; color: #fff; }
  .ts-story__body { margin-top: 12px; max-width: 440px; font-size: 16px; line-height: 1.6; color: #a3a3a3; }
  .ts-story__text--inline { margin-top: 20px; }
  @media (min-width: 750px) {
    .ts-story { display: grid; grid-template-columns: 1fr 1fr; gap: 64px; }
    .ts-story__sticky { position: sticky; top: 0; height: 100vh; display: flex; align-items: center; }
    .ts-story__textwrap { display: grid; }
    .ts-story__text { grid-area: 1 / 1; opacity: 0; transition: opacity .5s ease; }
    .ts-story__text.is-active { opacity: 1; }
    .ts-story__panel { min-height: 100vh; display: flex; flex-direction: column; justify-content: center; }
    .ts-story__title { font-size: 32px; }
  }

  /* ---- hero scroll: image grows slightly (no fade); floats scatter, converge into the
         screenshot centre and rack into focus (blur->0, scale->1) as --p goes 0->1 ---- */
  .ts-hero { --p: 0; }
  .ts-hero-img { transform: scale(var(--hs, 0.8)); transform-origin: center; will-change: transform; }
  .hero-floats { position: absolute; inset: 0; z-index: -1; pointer-events: none; }
  .hero-float { position: absolute; left: 50%; top: 34%; width: var(--w, 160px); aspect-ratio: 3 / 2; border-radius: 2px; border: 1px solid rgba(255,255,255,.08); background-size: cover; background-position: center;
    transform: translate(-50%, -50%)
      translate(calc(var(--x) * (1 - var(--p))), calc(var(--y) * (1 - var(--p))))
      translate(0, calc(var(--drop, 48vh) * var(--p)))
      rotate(calc(var(--r, 0deg) * (1 - var(--p))))
      scale(calc(var(--s, 1) + (1 - var(--s, 1)) * var(--p)));
    filter: blur(calc(var(--blur, 0px) * (1 - var(--p))));
    opacity: calc(0.6 + 0.4 * var(--p));
    will-change: transform, filter, opacity; }
  .hero-float--1 { --x: -42vw; --y: -8vh;  --blur: 7px;  --s: .62; --r: -6deg; --w: 200px; background-image: url('{{ 'raw-red.jpg' | asset_url }}'); }
  .hero-float--2 { --x: -30vw; --y: 10vh;  --blur: 2px;  --s: .90; --r: -3deg; --w: 240px; background-image: url('{{ 'converted.jpg' | asset_url }}'); }
  .hero-float--3 { --x: -14vw; --y: -20vh; --blur: 9px;  --s: .55; --r: 4deg;  --w: 170px; background-image: url('{{ 'raw-green.jpg' | asset_url }}'); }
  .hero-float--4 { --x: 15vw;  --y: -22vh; --blur: 8px;  --s: .58; --r: -4deg; --w: 176px; background-image: url('{{ 'raw-blue.jpg' | asset_url }}'); }
  .hero-float--5 { --x: 32vw;  --y: -4vh;  --blur: 3px;  --s: .88; --r: 5deg;  --w: 236px; background-image: url('{{ 'converted.jpg' | asset_url }}'); }
  .hero-float--6 { --x: 44vw;  --y: 12vh;  --blur: 7px;  --s: .64; --r: 7deg;  --w: 200px; background-image: url('{{ 'raw-red.jpg' | asset_url }}'); }
  .hero-float--7 { --x: -38vw; --y: 26vh;  --blur: 11px; --s: .50; --r: 6deg;  --w: 156px; background-image: url('{{ 'raw-green.jpg' | asset_url }}'); }
  @media (prefers-reduced-motion: reduce) {
    .hero-floats { display: none; }
    .ts-hero-img { transform: none; }
  }
{%- endstyle -%}

<script>
  (function () {
    document.querySelectorAll('.tonescan-landing .ts-ba').forEach(function (el) {
      var range = el.querySelector('.ts-ba__range');
      if (!range) return;
      var update = function () { el.style.setProperty('--pos', range.value + '%'); };
      range.addEventListener('input', update);
      update();
    });
  })();

  /* scan gallery lightbox */
  (function () {
    var lb = document.querySelector('.tonescan-landing .ts-lb');
    var scans = Array.prototype.slice.call(document.querySelectorAll('.tonescan-landing .ts-scan'));
    if (!lb || !scans.length) return;

    var wrap = lb.querySelector('.ts-lb__imgwrap');
    var keys = ['camera', 'lens', 'light', 'film', 'shot', 'format'];
    var idx = 0;
    var lastFocus = null;

    function render(i) {
      idx = (i + scans.length) % scans.length;
      var el = scans[idx];
      keys.forEach(function (k) {
        var dd = lb.querySelector('[data-k="' + k + '"]');
        if (dd) dd.textContent = el.getAttribute('data-' + k) || '—';
      });
      var img = el.getAttribute('data-img');
      var h = el.style.getPropertyValue('--h') || '0deg';
      wrap.innerHTML = img
        ? '<img src="' + img + '" alt="">'
        : '<span class="ts-scan__img" style="--h:' + h + '"></span>';
    }
    function open(i) {
      lastFocus = document.activeElement;
      render(i);
      lb.hidden = false;
      document.body.style.overflow = 'hidden';
      lb.querySelector('.ts-lb__close').focus();
    }
    function close() {
      lb.hidden = true;
      document.body.style.overflow = '';
      if (lastFocus && lastFocus.focus) lastFocus.focus();
    }

    scans.forEach(function (el, i) { el.addEventListener('click', function () { open(i); }); });
    lb.querySelectorAll('[data-close]').forEach(function (b) { b.addEventListener('click', close); });
    lb.querySelector('.ts-lb__prev').addEventListener('click', function () { render(idx - 1); });
    lb.querySelector('.ts-lb__next').addEventListener('click', function () { render(idx + 1); });
    document.addEventListener('keydown', function (e) {
      if (lb.hidden) return;
      if (e.key === 'Escape') close();
      else if (e.key === 'ArrowLeft') render(idx - 1);
      else if (e.key === 'ArrowRight') render(idx + 1);
    });
  })();

  /* rgb merge — scroll-linked: channels split when centred low, combine + reveal result as they scroll up */
  (function () {
    var els = document.querySelectorAll('.tonescan-landing .ts-rgb');
    if (!els.length) return;
    var clamp = function (v) { return v < 0 ? 0 : (v > 1 ? 1 : v); };
    var texts = ['Three RGB channel captures', 'Channels aligned & combined', 'Converted with tonescan'];

    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      els.forEach(function (el) {
        el.style.setProperty('--s', 0);
        el.classList.add('is-converted');
        var c = el.querySelector('.ts-rgb__cap'); if (c) c.textContent = texts[2];
      });
      return;
    }

    var items = [];
    els.forEach(function (el) { items.push({ el: el, cap: el.querySelector('.ts-rgb__cap'), last: -1 }); });

    function update() {
      var vh = window.innerHeight || document.documentElement.clientHeight;
      items.forEach(function (it) {
        var r = it.el.getBoundingClientRect();
        var p = clamp((vh - r.top) / (vh + r.height)); // 0 entering bottom -> 1 leaving top
        var s = 1 - clamp(p * 2);                        // spread 1..0 over first half (scrubbed)
        it.el.style.setProperty('--s', s.toFixed(3));
        var converted = p >= 0.5;                        // merge complete -> crossfade all at once
        it.el.classList.toggle('is-converted', converted);
        if (it.cap) {
          var ti = converted ? 2 : (p < 0.3 ? 0 : 1);
          if (ti !== it.last) { it.cap.textContent = texts[ti]; it.last = ti; }
        }
      });
    }

    // When scrolling settles near the merge point, snap so it never rests half-merged.
    function maybeSnap() {
      var vh = window.innerHeight || document.documentElement.clientHeight;
      var best = null, bestDist = 1;
      items.forEach(function (it) {
        var r = it.el.getBoundingClientRect();
        var p = clamp((vh - r.top) / (vh + r.height));
        var d = Math.abs(p - 0.5);
        if (d < 0.08 && d < bestDist) { bestDist = d; best = { top: r.top, h: r.height }; }
      });
      if (best && bestDist > 0.006) {
        var targetY = Math.round(window.pageYOffset + best.top - (vh - best.h) / 2);
        try { window.scrollTo({ top: targetY, behavior: 'smooth' }); }
        catch (e) { window.scrollTo(0, targetY); }
      }
    }

    var ticking = false, snapTimer;
    function onScroll() {
      if (!ticking) {
        ticking = true;
        requestAnimationFrame(function () { update(); ticking = false; });
      }
      clearTimeout(snapTimer);
      snapTimer = setTimeout(maybeSnap, 160);
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', function () { clearTimeout(snapTimer); update(); }, { passive: true });
    update();
  })();

  /* workflow story — the centred media panel drives which pinned text is shown */
  (function () {
    document.querySelectorAll('.tonescan-landing .ts-story').forEach(function (story) {
      var panels = story.querySelectorAll('.ts-story__panel');
      var texts = story.querySelectorAll('.ts-story__textcol .ts-story__text');
      if (!panels.length || !texts.length) return;
      function activate(i) {
        texts.forEach(function (t, ti) { t.classList.toggle('is-active', ti === i); });
      }
      if (!('IntersectionObserver' in window)) { texts[0].classList.add('is-active'); return; }
      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (e) {
          if (e.isIntersecting) activate(parseInt(e.target.getAttribute('data-i'), 10) || 0);
        });
      }, { rootMargin: '-45% 0px -45% 0px', threshold: 0 });
      panels.forEach(function (p) { io.observe(p); });
    });
  })();

  /* hero scroll — screenshot grows slightly; scattered floats converge inward (--p on .ts-hero) */
  (function () {
    var frame = document.querySelector('.tonescan-landing .ts-hero');
    if (!frame) return;
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    var img = frame.querySelector('.ts-hero-img');
    var clamp = function (v) { return v < 0 ? 0 : (v > 1 ? 1 : v); };
    var ticking = false;
    function update() {
      var vh = window.innerHeight || document.documentElement.clientHeight;
      var p = clamp(window.pageYOffset / (vh * 0.85));
      frame.style.setProperty('--p', p.toFixed(3));
      if (img) {
        // scale 0.8 -> 1.0, reaching full size once 70% of the image is in the viewport
        var r = img.getBoundingClientRect();
        var visible = Math.min(r.bottom, vh) - Math.max(r.top, 0);
        var f = r.height > 0 ? clamp(visible / r.height, 0, 1) : 0;
        var hs = 0.8 + 0.2 * clamp(f / 0.7, 0, 1);
        img.style.setProperty('--hs', hs.toFixed(3));
      }
    }
    function onScroll() {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(function () { update(); ticking = false; });
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', onScroll, { passive: true });
    update();
  })();
</script>

{% schema %}
{
  "name": "tonescan landing",
  "tag": "section",
  "class": "section",
  "settings": [],
  "presets": [{ "name": "tonescan landing" }]
}
{% endschema %}
